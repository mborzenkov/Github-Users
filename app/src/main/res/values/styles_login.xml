<resources>

    <!-- Styles XML для Login Fragment -->
    <style name="Login"/>
    <style name="Login.ButtonVk" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textSize">@dimen/size_text_secondary</item>
        <item name="android:textColor">@color/icons</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingLeft">@dimen/padding_login_button_vk</item>
        <item name="android:paddingRight">@dimen/padding_login_button_vk</item>
        <item name="android:drawablePadding">@dimen/padding_login_button_vk</item>
        <item name="android:text">@string/login_vk</item>
        <item name="android:background">@drawable/buttom_vk</item>
    </style>
    <style name="Login.ButtonCancel" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textSize">@dimen/size_text_small</item>
        <item name="android:textColor">@color/secondary_text</item>
    </style>

</resources>
