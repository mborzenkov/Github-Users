<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/height_block"
    android:layout_gravity="bottom"
    android:background="?android:attr/colorBackground"
    >

    <Button
        android:id="@+id/button_pagination_prev"
        android:layout_width="@dimen/size_squarebutton"
        android:layout_height="@dimen/size_squarebutton"
        android:layout_marginLeft="@dimen/marginhorizontal_normal"
        android:layout_marginStart="@dimen/marginhorizontal_normal"
        android:text="@string/pagination_button_prev"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

    <TextView
        android:id="@+id/tv_pagination_pages"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Page 1 / 30"
        />

    <Button
        android:id="@+id/button_pagination_next"
        android:layout_width="@dimen/size_squarebutton"
        android:layout_height="@dimen/size_squarebutton"
        android:layout_marginRight="@dimen/marginhorizontal_normal"
        android:layout_marginEnd="@dimen/marginhorizontal_normal"
        android:text="@string/pagination_button_next"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />


</android.support.constraint.ConstraintLayout>